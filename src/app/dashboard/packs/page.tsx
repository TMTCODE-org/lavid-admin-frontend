import PageContainer from '@/components/layout/page-container';
import PacksList from '@/features/packs/components/packs-list';

export default function PacksPage() {
  return (
    <PageContainer>
      <PacksList />
    </PageContainer>
  );
}
